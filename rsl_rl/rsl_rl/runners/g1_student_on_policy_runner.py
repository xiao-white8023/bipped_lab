class G1StudentOnPolicyRunner:
    def __init__(self, *args, **kwargs):
        raise RuntimeError(
            "g1_student was removed when keeping only g1_rough, g1_film, "
            "and g1_squart training tasks."
        )
